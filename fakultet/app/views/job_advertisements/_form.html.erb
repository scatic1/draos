<%= form_with(model: job_advertisement, local: true) do |form| %>
  <% if job_advertisement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_advertisement.errors.count, "error") %> prohibited this job_advertisement from being saved:</h2>

      <ul>
      <% job_advertisement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :work_position ,class:"control-label col-sm-2" %>
    <%= form.text_field :work_position,class:"form-control col-sm-2", id: :job_advertisement_work_position %>
  </div>
<br/>
  <div class="actions">
   <div class="form-group">
      <%= submit_tag 'Submit',class:"btn btn-dark"%>
    </div>
  </div>
<% end %>
